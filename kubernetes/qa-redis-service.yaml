apiVersion: v1
kind: Service
metadata:
    name: qa-redis-service
spec:
    selector:
        app: qa-redis
    ports:
        -   protocol: TCP
            port: 6379
            targetPort: 6379
    type: NodePort
  