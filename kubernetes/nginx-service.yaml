apiVersion: v1
kind: Service
metadata:
    name: nginx-service
    labels:
        app: nginx
        monitor: "true"
spec:
    type: LoadBalancer
    selector:
        app: nginx
    ports:
        - protocol: TCP
          port: 80
          targetPort: 7800
